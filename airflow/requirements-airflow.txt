# Subset des dépendances pipeline — sans streamlit/openpyxl/pytest (inutiles dans Airflow)

# --- API clients ---
openai>=1.0.0
anthropic>=0.20.0
google-search-results>=2.4.2

# --- Embeddings & vector search ---
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- HTTP & parsing ---
requests>=2.31.0
feedparser>=6.0.10
beautifulsoup4>=4.12.0

# --- Config ---
python-dotenv>=1.0.0
